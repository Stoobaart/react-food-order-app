# react-food-order-app
Practice React app that uses state, context and form actions (amongst other things)

To run the app, run npm install in the root directory, and do the same in the backend directory. Then run npm run dev in the root and npm start in the backend directory. that's all.
